namespace beemesh;

// CapacityReply schema (separate file so we can have per-file root_type)
table CapacityReply {
  ok:bool;
  node_id:string;
  region:string;
  capabilities:[string];
  cpu_available_milli:uint32;
  memory_available_bytes:ulong;
  storage_available_bytes:ulong;
}

root_type CapacityReply;
